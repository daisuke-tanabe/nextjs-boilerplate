type User {
  id: String
  name: String
}

type Book {
  id: String
  title: String
}

type Query {
  user: User
  books: [Book]
}

query Query {
  user {
    id
    name
  }
  books {
    id
    title
  }
}
